import React from 'react'
import Icon from 'supercons'

export default () => (
  <div style={{ color: 'red' }}>
    <Icon glyph="thumbsup" size={256} />
    <Icon glyph="like" size={128} />
    <Icon glyph="cloud" size={32} />
  </div>
)
